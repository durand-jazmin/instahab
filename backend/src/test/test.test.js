import assert from 'assert';

const x = 42;

// Verifica si la afirmación es verdadera, de lo contrario, arroja un error
assert.ok(x === 42, 'El valor de x debería ser 42');

console.log('La aserción pasó correctamente');